---
id: OPEN-USERS-ACCESS
name: Who else is capturing — Access or a shared passphrase?
summary: Determines whether Cloudflare Access is worth configuring or a shared passphrase suffices, which changes the M1 sign-in build.
status: open
date: 2026-08-30
milestone: next
order: 3
flags: sign-off
---
# Who else is capturing — Access or a shared passphrase?

Access is free to 50 users and means no password handling by us, which
is the reason to prefer it. But it is only worth configuring if more
than one person actually captures.

Jen appears in the source data (`Vinyl Records Record 2 Jen.xlsx`), so
the answer is probably "at least two" — but that is an inference from
a filename, not a decision.

**Maintainer decision required**, and it gates the M1 sign-in build.
