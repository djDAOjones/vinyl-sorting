---
id: M0-ARCHIVE-FREEZE
name: Archive and freeze the source spreadsheets
summary: Copy everything in Pre August 2026/ to a read-only archive and record checksums, so every later import has a fixed, provable starting point.
status: todo
milestone: current
order: 1
---
# Archive and freeze the source spreadsheets

`Pre August 2026/` becomes read-only for the life of the project (see
AGENTS.md hard rules). Take checksums of each source file so the
reconciliation report can name exactly which bytes it read.

Sources that matter: `Vinyl Records Record 2 Jen.xlsx` (the enriched
305), `1st load to add.xlsx`, `2nd load to add.xlsx`, and
`classical vinyl list in progress.csv`.

**Done when** a checksummed manifest of the frozen inputs exists.
